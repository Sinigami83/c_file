const n 19;
enum republic { Gruzia, Rossia, Ukrain };
enum city { Kiev, Moscow, Odessa, Sochi, Tbilisi, Tomsk };
enum city arr[n];

enum city found_max_city(enum city *arr)
{
    int i;
    enum city max = Kiev;
    for (i = 0; i <=n; i++) {

    }
    return *arr;
}

int main()
{
    found_max_city(*arr);   
    


    return 0;
}