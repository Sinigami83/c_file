#include <stdio.h>
int main()
{
    /*int x, n, k, t;
    x >= 0 && x <= 1;
    x <= 0 || x >= 1;
    (x >= 2 && x <= 5) || (x >= -1 && x <= 1);
    (x < 2 || x > 5) && (x < -1 || x > 1);

    if ( (n % 2 == 0) && (k % 2 == 0) )
      printf("Yes");
    else
      printf("No");

    t = (x == y) && (x == z);
    t = (x == y) || (x == z) || (y == z);
    t = x > 0;
    t = (q % p) == 0;*/

    int a;
    scanf("%d", &a);
    printf("%d\n", a);
    printf("%d %d\n", a * a * a, a * a * a);
    printf("%d %d %d\n", a * a * a * a * a * a, a * a * a, a);


    return 0;
}
